# Quant Trading Backend

This is the backend service for the Quant Trading application.

## Setup

1. **Virtual Environment**: A Python virtual environment is configured in `.venv`.
2. **Install Dependencies**:
   ```bash
   pip install -r requirements.txt
   ```
3. **Run Server**:
   ```bash
   python main.py
   ```
